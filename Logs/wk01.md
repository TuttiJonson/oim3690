# Week 1

## What I worked on
- Set up my toolchain with my GitHub account, Git, VS Code (or Cursor), GitHub Desktop
- Worked with a live installer in VS Code.
- Cloned locally, made index.html, committed ("create homepage"), pushed to origin
- Poked at the request flow myself using nslookup and traceroute on babson.edu, and counting network requests on a page with the ad blocker and how it changes 
- Inspect on my chrome page and discovered the underlining code and how the babson website was structured.

## What gave me trouble

- The reflex of commit and then push on Github Desktop as I hadn't necessarily worked on it beforehand. 
- I lost time wondering why my live page hadn't updated because I was committing and not pushing and commit is just local and wasn't shown on the live server 

## Something new I picked up

- Learned what "rendered" and "parse" mean.
- The basics of token math so five or six words cost approx 2,400 tokens in the demo, because the hidden system prompt plus the entire prior conversation rides along on every single turn
- What actually makes something an agent which is  the loop. Not that it answers questions but it keeps calling tools and re-evaluating until the task is done.
- The full request path so if I understood correctly: DNS turns babson.edu into an IP, the browser sends GET to an index.html, the server then returns HTML/CSS/JS, and the browser paints pixels.


## Something I noticed about working with AI this week

- I found I can open a file straight from the terminal inside VS Code just open e.g index.html
- The less context I gave, the more the output looked like every other AI-generated landing page 




